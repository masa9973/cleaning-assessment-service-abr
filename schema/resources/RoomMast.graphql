type RoomMast {
    roomID: ID!
    roomName: String!
    createdAt: AWSTimestamp!
    hotelID: ID!
    lastAssignedDate: String! # 最後にアサインした日
}

input RoomMastInput {
    roomID: ID!
    roomName: String!
    createdAt: AWSTimestamp!
    hotelID: ID!
    lastAssignedDate: String! # 最後にアサインした日
}