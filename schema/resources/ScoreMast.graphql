type  ScoreMast {
    recordID: ID!
    scoreID: ID!
    scorerUserID: ID!
    score: Int!
    createdAt: AWSTimestamp!
}

input ScoreMastInput {
    recordID: ID!
    scoreID: ID!
    scorerUserID: ID!
    score: Int!
    createdAt: AWSTimestamp!
}